"""Example module used by the pybundler demo."""


def greet(name: str) -> str:
    """Return a greeting for the given name.

    Args:
        name (str): Person's name.

    Returns:
        str: Greeting message.

    """
    return f"Hello, {name}!"
