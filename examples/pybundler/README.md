PyBundler example project

This directory demonstrates a minimal project layout and a `.bundler.json`.
Run the bundler from this folder to see how files are collected and the TOC is used.

Example:
  python -m pybundler --root . --config .bundler.json --toc --output EXAMPLE_BUNDLE.md
